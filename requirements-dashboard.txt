# 결과 비교 대시보드 전용 (streamlit run app.py). 로컬에서만 설치하면 된다.
#
#   pip install -r requirements-dashboard.txt
#
# GPU 파드에는 넣지 말 것 — streamlit 이 끌어오는 blinker 가 apt(distutils)로
# 이미 깔려 있어서 pip 이 제거하지 못하고 설치 전체가 실패한다.
# 파드에서는 requirements.txt 만 설치하면 벤치마크가 돌아간다.

# 1.50 미만에서는 width="stretch" 를 못 알아듣는다 (구 use_container_width).
streamlit>=1.50.0

# app.py 가 summary.csv / details.json / chunks.json 을 읽는 데 쓴다.
# (altair 는 streamlit 이 함께 설치한다)
pandas>=2.0.0
