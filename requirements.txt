# 대시보드(app.py) 전용 — Streamlit Community Cloud 가 이 파일을 읽는다.
#
# Community Cloud 는 저장소 루트의 requirements.txt 를 자동으로 설치하며
# 파일명을 바꿀 방법이 없다. 그래서 여기엔 대시보드에 필요한 것만 둔다.
# 벤치마크 실행 의존성(torch, transformers, FlagEmbedding …)은
# requirements-runpod.txt 에 있고, GPU 파드에서만 설치한다:
#
#     pip install -r requirements-runpod.txt
#
# 여기에 sentence-transformers 같은 걸 넣으면 torch 까지 딸려와서
# Community Cloud 리소스 한도를 넘겨 배포가 실패한다.

streamlit>=1.50.0     # st.context.theme, width="stretch" 사용
pandas>=2.0.0         # summary.csv / details.json / chunks.json 읽기
# altair 는 streamlit 이 함께 설치하므로 명시하지 않는다
