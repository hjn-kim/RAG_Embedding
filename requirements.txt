# ── RunPod (Linux + CUDA 12.x) 기준 ───────────────────────────────
# RunPod PyTorch 템플릿을 쓰면 torch는 이미 설치되어 있으므로
# 아래 torch 라인은 주석 처리해도 됩니다. (버전 충돌 방지)
# torch>=2.1.0

# 임베딩 모델
sentence-transformers>=3.0.0
transformers>=4.44.0
huggingface-hub>=0.24.0
accelerate>=0.30.0

# BGE-M3 hybrid(dense+sparse) 평가용. 설치 실패해도 dense 단독으로는 동작함.
FlagEmbedding>=1.3.0

# 문서 파싱
python-docx>=1.1.0          # .docx
PyMuPDF>=1.24.0             # .pdf (주 파서)
pypdf>=4.2.0                # .pdf (fallback)

# 유틸
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.66.0
PyYAML>=6.0
tabulate>=0.9.0
